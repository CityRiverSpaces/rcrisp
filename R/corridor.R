corridor <- function(network, corridor_guess, river_centerline, params = NULL, capping_method = NULL) {
  edges <- corridor_edges()
  corridor <- cap_corridor(edges, capping_method)
  corridor
}

corridor_edges <- function() {


}

cap_corridor <- function(edges, capping_method) {

}