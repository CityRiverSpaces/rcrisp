% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delineation-methods.R
\name{summary.delineation}
\alias{summary.delineation}
\title{Summarise a delineation object}
\usage{
\method{summary}{delineation}(object, ...)
}
\arguments{
\item{object}{An object of class \code{delineation}.}

\item{...}{Not used; included for compatibility with the generic.}
}
\value{
An object of class \code{summary.delineation}, which is a named list
with elements \code{city_name}, \code{river_name}, \code{crs}, \code{parameters},
\code{delineation_layers}, and \code{base_layers}. \code{parameters} contains
\code{network_buffer}, \code{dem_buffer}, and \code{buildings_buffer} (in metres), or
\code{NULL} if no \code{aoi} was stored. Each layer entry is itself a named list
of stats, or \code{NULL} if the layer is absent.
}
\description{
Computes summary statistics for a \code{delineation} object and returns them as
a structured list of class \code{summary.delineation}. Delineation layers are
reported with their area (in km²). For \code{segments}, the number of features
and mean area are also given. Base layers are reported with feature counts;
\code{river_centerline} as total length (in km) and \code{river_surface} as area
(in km²).
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
bd <- delineate_city_river("Bucharest", "Dâmbovița")
s <- summary(bd)
s$delineation_layers$corridor$area_km2
\dontshow{\}) # examplesIf}
}
