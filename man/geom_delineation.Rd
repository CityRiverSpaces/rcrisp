% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation.R
\name{geom_delineation}
\alias{geom_delineation}
\title{ggplot2 layer for a delineation object}
\usage{
geom_delineation(x, extent = "corridor", legend = TRUE)
}
\arguments{
\item{x}{An object of class \code{delineation}. This is typically the output
of the \code{\link[=delineate]{delineate()}} function.}

\item{extent}{The delineation layer whose bounding box sets the plot extent.
One of \code{"corridor"}, \code{"valley"}, \code{"riverspace"}, or \code{NULL}
(no restriction, ggplot2 fits all layers). Defaults to \code{"corridor"}.}

\item{legend}{Whether the plot should display a legend. Default is TRUE.}
}
\value{
A list of \code{\link[ggplot2:ggsf]{ggplot2::geom_sf()}} and \code{\link[ggplot2:ggsf]{ggplot2::coord_sf()}} elements
to be added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Creates a list of \code{\link[ggplot2:ggsf]{ggplot2::geom_sf()}} layers representing the delineation
results of a delineation object. The list can be added to a
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object with \code{+}, and further layers or themes can be
added on top. Base layers (\code{streets}, \code{railways}, \code{river_centerline},
\code{river_surface}) are not included and can be added separately with
\code{\link[ggplot2:ggsf]{ggplot2::geom_sf()}}.
}
\details{
The following layers are included (where present in \code{x}), in the following
order:
\itemize{
\item \code{corridor} or \code{riverspace} (only when \code{corridor} is absent):
drawn first to set the plot extent
\item \code{valley}: light grey, transparent fill, no outlines
\item \code{segments}: outlined polygons
\item \code{corridor}: outlined polygon
}
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(ggplot2)
bd <- delineate_city_river("Bucharest", "Dâmbovița")
ggplot() + geom_delineation(bd) + theme_void()
ggplot() + geom_delineation(bd, extent = "valley") + theme_void()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot.delineation]{plot.delineation()}} for a base R alternative.
}
