# Check cache

A warning is raised if the cache size is \> 100 MB or if it includes
files older than 30 days.

## Usage

``` r
check_cache()
```

## Value

`NULL`

## Examples

``` r
if (FALSE) { # interactive()
check_cache()
}
```
